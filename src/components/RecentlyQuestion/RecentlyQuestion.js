import classNames from "classnames/bind";
import styles from './RecentlyQuestion.module.scss'

const cx = classNames.bind(styles)

function RecentlyQuestion() {
    return ( 
        <div className={cx('wrapper')}>blog pages</div>
     );
}

export default RecentlyQuestion